name: ETAbot Replit ping

on:
  schedule:
    - cron: "*/5 * * * *" # every 5 minutes
  workflow_dispatch:      # allows manual runs

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ETAbot Replit ping
        run: |
          curl -s https://fd3e4f2f-5b0b-4545-a03a-80dd194e74b1-00-3n609182pygq.worf.replit.dev/ > /dev/null